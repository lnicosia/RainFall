void p(void)
{
	uint32_t ret;
	char buf [76];

	fflush(stdout);
	gets(buf);
	ret = $eip;
	if ((ret & 0xb0000000) == 0xb0000000) {
		printf("(%p)\n", ret);
		_exit(1);
	}
	puts(buf);
	strdup(buf);
	return;
}

void main(void)
{
	p();
	return;
}
